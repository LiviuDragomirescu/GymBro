import { View } from 'react-native';

import Template_Button from '../components/Template_Button';



const Login = ({navigation}) => {
    return (
      <View>
        <Template_Button navigation={navigation} ></Template_Button>
      </View>
    )
  }

  export default Login;